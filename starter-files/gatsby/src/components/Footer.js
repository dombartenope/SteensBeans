import React from 'react';

const Footer = () => {
    return (
        <footer className = "center">
            <small>Steen's Beans {new Date().getFullYear()}</small>
        </footer>
    )
};
export default Footer;
